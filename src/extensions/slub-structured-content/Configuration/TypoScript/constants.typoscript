plugin.tx_structuredcontent {
  persistence {
    # cat=plugin.tx_structuredcontent//a; type=string; label=Default storage PID
    storagePid =
    # cat=plugin.tx_structuredcontent//a; type=int; label=UID of the page/folder where all new records will be created
    newRecordStoragePid =
  }
}
