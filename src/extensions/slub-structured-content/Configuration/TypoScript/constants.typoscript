plugin.tx_structuredcontent {
  persistence {
    # cat=plugin.tx_slub_structured_content//a; type=string; label=Default storage PID
    storagePid =
    # cat=plugin.tx_slub_structured_content//a; type=int; label=UID of the page/folder where all new records will be created
    newRecordStoragePid =
  }
}
